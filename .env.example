# YesLocker Environment Variables Template
# Copy this file to .env.local and fill in your values

# =============================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# =============================================================================

# Node Environment
NODE_ENV=development

# Frontend Development Server
VITE_PORT=3000
VITE_API_BASE_URL=http://localhost:3001

# Backend Development Server  
PORT=3001

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Development (SQLite)
DATABASE_TYPE=sqlite
DATABASE_PATH=./server/database/yeslocker.db

# Production (PostgreSQL) - Railway will auto-populate DATABASE_URL
# DATABASE_URL=postgresql://username:password@host:port/database

# =============================================================================
# SMS SERVICE CONFIGURATION (OPTIONAL)
# =============================================================================

# Tencent Cloud SMS Service
# Get these from: https://console.cloud.tencent.com/smsv2
TENCENT_SECRET_ID=your_secret_id_here
TENCENT_SECRET_KEY=your_secret_key_here
TENCENT_SMS_APP_ID=your_app_id_here
TENCENT_SMS_SIGN_NAME=your_sign_name_here

# SMS Templates (optional)
TENCENT_SMS_TEMPLATE_LOGIN=your_login_template_id
TENCENT_SMS_TEMPLATE_REGISTER=your_register_template_id

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# JWT Secret for token signing (generate a secure random string)
JWT_SECRET=your_super_secret_jwt_key_here_please_change_this

# Admin Panel Security
ADMIN_SECRET_KEY=your_admin_secret_key_here

# =============================================================================
# PRODUCTION DEPLOYMENT VARIABLES
# =============================================================================

# These are automatically set by Railway, but you can override if needed

# Application URL (Railway auto-generates)
# VITE_API_BASE_URL=https://your-app-name.railway.app

# Database (Railway auto-provides)
# DATABASE_URL=postgresql://...

# =============================================================================
# OPTIONAL FEATURES
# =============================================================================

# Enable/disable features
ENABLE_SMS_VERIFICATION=false
ENABLE_EMAIL_NOTIFICATIONS=false
ENABLE_PUSH_NOTIFICATIONS=false

# Logging level
LOG_LEVEL=info

# File upload limits
MAX_AVATAR_SIZE=2MB
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif

# =============================================================================
# DEVELOPMENT TOOLS
# =============================================================================

# Debug mode
DEBUG=false
VERBOSE_LOGGING=false

# Hot reload for development
HOT_RELOAD=true

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local:
#    cp .env.example .env.local

# 2. Fill in your values, especially:
#    - JWT_SECRET (generate a random string)
#    - TENCENT_* values (if using SMS)
#    - DATABASE_URL (for production)

# 3. Never commit .env.local to git!

# 4. For Railway deployment, set production variables via:
#    railway variables set KEY=value

# 5. For local development, the app will use SQLite by default
#    For production, it will use PostgreSQL from DATABASE_URL